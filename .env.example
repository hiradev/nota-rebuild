# Base URL of the Strapi CMS (no trailing slash)
NEXT_PUBLIC_STRAPI_URL=http://localhost:1337

# Server-only, read-only Strapi API token (Settings -> API Tokens in the Strapi admin)
STRAPI_API_TOKEN=
